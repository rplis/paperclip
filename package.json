{
  "name": "paperclip-lean",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel --filter @lean/api --filter @lean/ui dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.4"
}
